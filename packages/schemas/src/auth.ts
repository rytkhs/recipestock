// パスワードの長さ。APIのbetter-authの設定と、画面の入力欄・文言が同じ値を使う。
export const MIN_PASSWORD_LENGTH = 8;
export const MAX_PASSWORD_LENGTH = 128;

// メールアドレスの変更で送る確認リンクの有効期限。APIのbetter-authの設定と、確認メール・画面の文言が同じ値を使う。
export const EMAIL_CHANGE_LINK_EXPIRES_IN_HOURS = 1;
